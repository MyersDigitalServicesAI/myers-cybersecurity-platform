# Web Frameworks and Servers
streamlit==1.33.0          # For interactive dashboards and UI (your frontend)
fastapi~=0.111.0           # For building your backend RESTful APIs (recommended)
uvicorn[standard]~=0.30.0  # ASGI server to run FastAPI applications
Flask                      # Alternative Python web framework (WSGI), or for specific Flask components
gunicorn                   # WSGI HTTP server for deploying Flask apps, or for managing Uvicorn workers for FastAPI

# Database and ORM
psycopg2-binary==2.9.9     # PostgreSQL database adapter (binary distribution for easier install)
supabase==2.1.0            # Supabase Python client library
sqlalchemy==2.0.30         # SQL Toolkit and Object Relational Mapper

# Security and Authentication
bcrypt==4.1.2              # For secure password hashing
cryptography==42.0.5       # Cryptographic primitives (often a dependency for other security libs)

# Email and Communication
email-validator==2.1.1     # For validating email addresses
sendgrid==6.11.0           # SendGrid API client for sending emails

# HTTP Client
requests==2.31.0           # Popular HTTP client library for making web requests
ratelimit==2.2.1           # For rate limiting logic (login/signup protection)

# Environment Variables
python-dotenv==1.0.1       # For loading environment variables from .env files

# Payments
stripe==9.7.0              # Stripe API client for payment processing

# Data Visualization
plotly==5.21.0             # For interactive plotting and data visualization
